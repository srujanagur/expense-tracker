export type Labels = {
	name: string;
	amount: number;
	type: string;
	color: string;
	_id: string;
};

export type dataPercent = {
	type: string;
	color: string;
	percent: number;
	total: number;
}[];
